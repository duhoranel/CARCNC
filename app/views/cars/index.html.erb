<h1>Car's list </h1>
<p>Here's the list of the available cars :</p>

<ul>
  <% @cars.each do |car| %>
    <%= link_to car.model, car_path(car) %>
  <% end %>
</ul>

<%= link_to 'Add a new car', new_car_path %>